<%= form_with model: task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %><% unless f.object.new_record? %>
    <%= f.check_box :completed %> Completed
  <% end %>
  <%= f.submit %>
<% end %>
